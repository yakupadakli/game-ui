/// Package name for bundled [Image.asset] / [AssetImage] lookups.
const String gameUiPackageName = 'game_ui';

/// Root directory for all bundled image assets.
const String kGameAssetRoot = 'assets/images';

/// Default font size shared across game-style widgets.
const double kGameDefaultFontSize = 16;
